export type GameMap = {
    index: number;
    name: string;
    homeScore: number;
    awayScore: number;
    pickedBy: string;
    game: string;
    decider: boolean;
    upNext: boolean;
    notNeeded: boolean;
};
